# Generated by Claude — CS 3660 Sprint 2
# Caddyfile: reverse proxy for fleet-monitor dashboard + WebSocket bridge.
# Replace fleet.yourdomain.com with your actual domain or server IP.
# Caddy auto-provisions a Let's Encrypt TLS cert for any real domain.

fleet.yourdomain.com {
    # Serve the ws-bridge (Koa) on all paths, including WebSocket upgrades.
    # Caddy automatically handles the wss:// upgrade over HTTPS.
    reverse_proxy localhost:3000
}
